require 'spec_helper'

describe Toy::Robot::Simulator do
  it 'has a version number' do
    expect(Toy::Robot::Simulator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
